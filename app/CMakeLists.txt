#cmake_minimum_required(VERSION 3.4.1)
#
#add_library(libDeviceConfig SHARED IMPORTED)
#set_target_properties(libDeviceConfig PROPERTIES IMPORTED_LOCATION ${CMAKE_SOURCE_DIR}/src/main/jniLibs/${ANDROID_ABI}/libDeviceConfig.so)
